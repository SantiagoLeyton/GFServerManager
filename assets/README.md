Coloque aqui el icono definitivo como `app.ico`.

La aplicacion lo cargara automaticamente en la ventana principal cuando exista, y la misma ruta queda preparada para una futura compilacion con PyInstaller.
